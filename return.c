#include<stdio.h>
#include<conio.h>
void main()
{
int K,arr[50],s,no;
scanf("%d",&no);
for(K=0;K<no;k++);
{
scanf("%d",&array[K]);
}
for(K=0;k<no;k++)
{
for(s=k+1;s<no;s++)
{
if(arr[k]==arr[s])
{
printf("%d\n",arr[k]);
}
}
}
getch();
}
